supported-domain-regions:
  domainRegions:
    - regionCode: usa
      localityCodes: ${USA_SUPPORTED_DOMAINS:la}
    - regionCode: switzerland
      localityCodes: ${SWITZERLAND_SUPPORTED_DOMAINS:ge}
    - regionCode: abc
      localityCodes: ${ABC_SUPPORTED_DOMAINS:xyz}
